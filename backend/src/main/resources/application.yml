server:
  port: ${SERVER_PORT:8080}

app:
  cors:
    allowed-origin: http://localhost:5173

gemini:
  api-key: AIzaSyB88Avj8r4gqkCR7NxPet-m9H3jnPLdYSg
  # Note: model list evolves over time. If chat replies fail with "model not found",
  # call `GET https://generativelanguage.googleapis.com/v1beta/models?key=...`
  # and pick a model that supports generateContent.
  model: gemini-2.5-flash
  base-url: https://generativelanguage.googleapis.com

spring:
  application:
    name: ai-chatbot-backend
